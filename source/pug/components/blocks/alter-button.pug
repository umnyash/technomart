mixin alter-button(tag, text, type, href, target, rel)
  if tag === 'button'
    button.alter-button(type= type)&attributes(attributes)= text
  else if tag === 'a'
    a.alter-button(href= href target= target rel= rel)&attributes(attributes)= text
